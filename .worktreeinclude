# Gitignored files copied into each new worktree (Claude Code feature).
# DO NOT add .env.local — it is per-branch and regenerated by `predev`
# (scripts/provision-db.ts). Copying it would leak the parent branch's
# DATABASE_URL/BETTER_AUTH_URL into the new worktree.
.env
